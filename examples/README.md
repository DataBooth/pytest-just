# Examples
This directory contains sample justfiles to use in pytest-just development and testing.

## Layout
- `public/`: samples adapted from publicly available justfiles
- `local/`: copied `../*/justfile` and `../*/Justfile` files from sibling repositories

## Note
Samples are intentionally compact and focused on parser/testing behaviours (dependencies, parameters, variables, private recipes, shebang, and imports).
